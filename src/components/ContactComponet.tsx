import React from "react";

interface IContactComponentProps {}

export default class ContactComponent extends React.Component<IContactComponentProps> {
  render() {
    return (
     
        <p>Contact</p>
      
    );
  }
}